<p id="notice"><%= notice %></p>

<p>
  <strong>Courseeeeeeeee id:</strong>
  <%= @course.course_Id %>
</p>

<p>
  <strong>Description:</strong>
  <%= @course.description %>
</p>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>



<table>
  <thead>
    <tr>
      <th>Student_ID</th>
      <th>Percentage</th>
      <th>Grade</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @studentts.each do |studentt| %>
      <tr>
        <td><%= coursees.student_id %></td>
        <td><%= coursees.percentage %></td>
        <td><%= coursees.name %></td>
              </tr>
    <% end %>
  </tbody>
</table>
