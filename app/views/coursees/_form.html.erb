<%= form_for(@coursee) do |f| %>
  <% if @coursee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coursee.errors.count, "error") %> prohibited this coursee from being saved:</h2>

      <ul>
      <% @coursee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %><br>
    <%= f.text_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
